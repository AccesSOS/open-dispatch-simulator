node_modules/
dist/
# Licensed/proprietary packs loaded locally — never committed. See docs/PRIVATE-PACKS.md.
packs-private/
*.log
.env
.DS_Store
# Real-call case files for replay validation — never committed. See docs/REPLAY.md.
replay-private/
